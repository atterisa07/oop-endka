# Полная инструкция по настройке проекта

## ✅ Что уже сделано:

1. ✅ Все Java файлы созданы (8 файлов)
2. ✅ SQLite JDBC драйвер скачан (`sqlite-jdbc.jar`)
3. ✅ Скрипты компиляции и запуска созданы
4. ✅ README.md с документацией создан

## 📋 Что нужно сделать:

### Шаг 1: Установка JDK (если еще не установлен)

JDK уже начал устанавливаться через winget. Чтобы завершить установку:

1. **Дождитесь завершения установки** (может занять несколько минут)
2. **Перезапустите терминал/PowerShell** после установки
3. **Проверьте установку:**
   ```powershell
   javac -version
   ```
   Должно вывести что-то вроде: `javac 1.8.0_xxx`

Если JDK не установлен, выполните:
```powershell
winget install AdoptOpenJDK.OpenJDK.8
```

### Шаг 2: Компиляция проекта

После установки JDK и перезапуска терминала:

**Вариант A - PowerShell:**
```powershell
.\compile.ps1
```

**Вариант B - Batch файл:**
```cmd
compile.bat
```

**Вариант C - Вручную:**
```cmd
javac -cp sqlite-jdbc.jar *.java
```

### Шаг 3: Запуск приложения

После успешной компиляции:

**Вариант A - PowerShell:**
```powershell
.\run.ps1
```

**Вариант B - Batch файл:**
```cmd
run.bat
```

**Вариант C - Вручную:**
```cmd
java -cp .;sqlite-jdbc.jar LibraryApp
```

## 📁 Структура проекта:

```
cursor talshyn/
├── DatabaseConnection.java    # Подключение к БД
├── Student.java                # Модель студента
├── Book.java                   # Модель книги
├── Loan.java                   # Модель займа
├── StudentDAO.java             # DAO для студентов
├── BookDAO.java                # DAO для книг
├── LoanDAO.java                # DAO для займов
├── LibraryApp.java             # Главное приложение
├── sqlite-jdbc.jar             # SQLite драйвер (уже скачан)
├── compile.bat                 # Скрипт компиляции (Windows)
├── compile.ps1                 # Скрипт компиляции (PowerShell)
├── run.bat                      # Скрипт запуска (Windows)
├── run.ps1                      # Скрипт запуска (PowerShell)
├── README.md                    # Документация проекта
├── SETUP.md                     # Эта инструкция
└── .gitignore                   # Игнорируемые файлы для Git
```

## 🎯 Все функции реализованы:

### Level 1 ✅
- Список всех студентов
- Добавление студента
- Поиск студента по ID
- Список всех книг
- Добавление книги
- Поиск книги по ID

### Level 2 ✅
- Список всех займов
- Добавление нового займа
- Возврат книги

### Level 3 ✅
- Список студентов, которые брали конкретную книгу
- Список не возвращенных книг студента
- Общее количество прочитанных страниц студента

### Level 4 ✅
- Самая популярная книга (больше всего займов)
- Студент с наибольшим количеством займов
- Средняя длительность займа для книги

## ⚠️ Важные замечания:

1. **База данных создается автоматически** при первом запуске (`library.db`)
2. **Формат даты:** YYYY-MM-DD (например, 2025-02-13)
3. **Книга может быть выдана только одному студенту** одновременно
4. **Все файлы .class** будут созданы при компиляции (игнорируются Git)

## 🐛 Решение проблем:

**Проблема:** `javac: command not found`
- **Решение:** Установите JDK и перезапустите терминал

**Проблема:** `Error: Could not find or load main class LibraryApp`
- **Решение:** Убедитесь, что компиляция прошла успешно и файлы .class созданы

**Проблема:** `java.sql.SQLException: no such table`
- **Решение:** Удалите файл `library.db` и запустите приложение снова (БД создастся заново)

## ✨ Проект готов к использованию!

Все файлы созданы, драйвер скачан. Осталось только установить JDK (если еще не установлен) и скомпилировать проект.
